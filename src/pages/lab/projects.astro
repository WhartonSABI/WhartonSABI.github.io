---
import Layout from '../../layouts/Layout.astro';
import Nav from '../../components/Nav.astro';
import ProjectCard from '../../components/ProjectCard.astro';
import { loadRepos, type Project } from '../../lib/data';

const repos = loadRepos();
const config = repos.lab;
const org = config.org;
---

<Layout title="Summer Lab – Projects" description="Summer research lab projects with presentations and papers.">
  <Nav />
  <main class="mx-auto max-w-5xl px-6 py-16">
    <h1 class="text-3xl font-bold text-stone-900">Summer Lab</h1>
    <p class="mt-2 text-stone-600">Summer research lab projects with presentations and papers.</p>

    <div class="mt-12 grid gap-6 sm:grid-cols-2">
      {config.projects.map((p: Project) => (
        <ProjectCard
          name={p.name}
          description={p.description}
          org={p.org_override || org}
          repo={p.repo}
          deliverables={p.deliverables}
        />
      ))}
    </div>
  </main>
</Layout>
